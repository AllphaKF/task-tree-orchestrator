---
name: task-tree-orchestrator
description: >
  在 task-tree-orchestrator 协议基础上，新增多执行者多头并行推进机制。
  大型任务规范化编排协议：以「任务结构树」为唯一事实源，将工作拆分为管理者大模型（统筹规划）与
  执行者大模型（落实呈递）双角色，通过轮次提示词与定时任务巡检完成对接。触发条件：
  (1) 用户说「用任务树」「双角色编排」「管理者/执行者模式」做某个大型任务；
  (2) 任务规模大（多模块、多阶段、预计多轮次）且用户指定了执行者大模型（可能是别的公司/别的产品的模型）；
  (3) 用户要求对既有大型任务建立结构树进行规范化管理。
  一旦调用本 skill，必须严格按照本 skill 内容执行，不得简化、跳过或自行变通流程。
  NOT for: 单文件小修小补、一次性问答、无多轮协作需求的普通编码任务。
  English triggers: "use a task tree", "dual-role orchestration", "manager/executor model",
  "structure a large task with branches", "round-based agent coordination", "patrol/inspection protocol",
  "multi-executor parallel", "parallel branches".
---

# 任务树编排协议（task-tree-orchestrator）

**多执行者并行机制**：本协议包含多执行者并行推进机制（条款编号 P0–P7，以【P<n>】标识）。默认全部枝同组串行；并行是显式声明，不是默认。单执行者任务可沿用串行模式与单文件 todolist。

**本 skill 是强制性协议，不是建议清单。** 一旦调用，从草创到收尾的每一步都必须严格按本文执行。若某步骤因现实条件无法执行（如执行者通道不可用），必须显式记录「哪一步、为什么无法执行、何时补齐」，不得静默跳过。

## 一、角色定义（三者权责不可混用）

> **【P7 指引】** 管理者之间存在上下级关系时（父管理者 / 子管理者），见本节末 `【P7】` 管理者层级（**双向兼容接口**：向上 / 向下）。

| 角色 | 承担者 | 权责 |
|---|---|---|
| **管理者大模型** | 本 skill 的调用者（即 Claude 本体） | 统筹规划：生成/裁剪/调整任务树 branch、制定各 branch 对接方案、定期检查执行者工作、汇总决策清单呈递人类、把人类的批复转达为下一轮任务 |
| **执行者大模型** | 由人类分配（可能是不同公司、不同产品的大模型，如 Codex/GPT/Claude 等，人类自定） | 只做两件事：① 按轮次提示词落实具体任务；② 把结果（含等待项、待授权项）呈递回树。其余一律按安排执行，**不允许越权** |
| **人类** | 用户 | 分配各级模型、裁决需要授权的重大事项、配置对接通道（定时任务/skill/手工投递）；可授权管理者调度权——更换执行者、更换模型、调整思维强度 |

**模型层级分配**：一个大型任务可以有多个执行者（不同 branch 分给不同模型）。管理者必须为每个执行者建立独立的「执行者档案」（见 §六），记录其通道、能力边界、已知环境坑。层级分配决策权在人类，管理者只负责按分配结果调度。

【P0】**执行者角色分级**：执行者分两类——①**作业执行者**：干任务本体的活，产出落自己的临时写区（见铁律 2）；②**秘书执行者**：专职机械动作（写区合并、文件搬运、发布、写区删除），由人类分配或管理者在调度权内指派低复杂度模型担任。**管理者只核验、只下指令，不亲手干活**（铁律 3 原样保持）。

【P7】**管理者层级（双向兼容接口：向上 / 向下）**：本协议支持**管理者之间的上下级关系**，作为跨树 / 跨工程协作的**双向兼容接口**——**向上一侧**（本管理者作为子管理者时的义务，见 A 组）与**向下一侧**（本管理者作为父管理者时的义务，见 B 组）成对规定。条文如下：

**A. 子管理者一侧（向上兼容）**

1. **多管理者层级**：协议支持**多管理者层级**——一个管理者可接受另一管理者作为**上级（父管理者）**下达命令；该关系**须经人类授权**，并登记于**双方各自的** B0 授权台账（**各树记各树的**）。
2. **父管理者权限（近似人类）**：在持有该人类授权的前提下，父管理者权限**近似人类**——可操控子管理者的**非树规则行为**（任务安排、执行者调配、操作性决策、工作顺序）。
3. **不可越界（铁律保护）**：父管理者**不得修改子管理者的树 skill 铁律与协议本体规则**（`§四 五条铁律`、审计只追加、先审后合、独立核验、呈递格式等）；**触碰铁律的命令，子管理者不动手、先呈递人类**。
4. **优先级**：**人类命令 > 父管理者命令 > 子管理者自主判断**；冲突时**以人类命令为准**。
5. **记录与落地**：层级关系与授权范围记入**子管理者自己的** B0 授权台账（**不得写对方树的治理文件**）；父管理者命令的执行仍走**子树的协议流程**（轮次提示词 → 执行者 → 核验 → 落账子树 → 双向呈报），且落地动作**由执行者完成**（铁律 3：管理者只核验、只下指令，不亲手干活）。
6. **典型用途**：跨工程 / 跨树协作（如**子树的成果并入父树**、**父树指挥子树做专项工作**）。

**B. 父管理者一侧（向下兼容）**

7. **授权前提与记录**：父管理者须**经人类授权**方可对子管理者行使上级权限；层级关系与授权范围记入**父管理者自己的** B0 授权台账（**不得代写子树治理文件**，各树记各树的）。
8. **权限范围（同向上）**：在授权范围内可操控子管理者的**非树规则行为**（任务安排、执行者调配、操作性决策、工作顺序）。
9. **下令前自检（铁律保护的对侧义务）**：**不得要求子管理者违反其树 skill 铁律与协议本体规则**（`§四 五条铁律`、审计只追加、先审后合、独立核验、呈递格式等）——**下令前须自检**；**触碰子树铁律的命令不得下达**；若已下达，子管理者**有权拒绝并呈递人类**，**父管理者不得因此绕过或追责**。
10. **不越级指挥**：**不得越过子管理者直接指挥其执行者**；指挥链走**子树管理者**，落地动作由**子树的执行者**完成（铁律 3：管理者只核验、只下指令，不亲手执行）。
11. **不与人类命令冲突**：知悉人类向子管理者下达的命令时，**以人类为准**；发现冲突**立即止令并呈递人类**。
12. **呈报与审计边界**：子管理者向父管理者呈报、父管理者向人类呈报；**父管理者的操作不改变子树的审计链**（子树只记录自己的事实）。

## 二、任务结构树（唯一事实源）

树存于项目内的 `task-tree/` 目录（草创时建立）。**树是管理者、执行者、人类三方共享的唯一事实源**——谁干了什么、卡在哪、等什么，全部以树文件为准，不以任何聊天记录为准。

### 2.1 预埋骨架（草创时一次性建好，即使当时 branch 很少）

**铁律：草创阶段必须把以下支撑性 branch 全部建好占位文件（含 limitations），即使它们此刻没有活。** 不允许"等需要时再建"——预埋的结构让未来扩展、并行执行、裁剪都有明确落点。

```
task-tree/
├── TREE.md                 # 树总览：目标、branch 图、状态表、对接表、决策清单（见 references/tree-template.md）
├── WORKER_PROTOCOL.md      # 执行者协议全文（按 references/worker-protocol.md 生成，每轮提示词要求执行者读它）
├── branches/
│   ├── B0-governance.md    # 治理枝：轮次日志、授权台账、决策记录（管理者专用，执行者只读）
│   ├── B1-scope.md         # 需求/范围枝：需求澄清、范围界定、验收标准
│   ├── B2-design.md        # 设计/方案枝：方案设计、技术选型、接口定义（superpowers 的 brainstorm/plan 产物落在此处）
│   ├── B3-impl-*.md        # 实现枝：按模块/功能拆多个（B3-impl-core、B3-impl-ui…），按需生成，可裁剪
│   ├── BV-verify.md        # 验证/审查枝：测试、审查、核验、收尾验收
│   └── BD-deliver.md       # 交付/文档枝：交付物整理、文档、发布、汇报
├── executors/              # 执行者档案：每个执行者一份（通道/状态判定/能力边界/环境坑），见 §六
├── interfaces.md           # 对接方案：枝间数据流、上游产出→下游依赖、接口格式约定
├── plans/                  # 方案/设计产出落点：设计枝的定稿方案与模板产物（核验通过后由秘书执行者合并至此）
├── reviews/                # 核验/盘点报告落点：盘点报告、校对报告等（核验通过后由秘书执行者合并至此）
├── staging/                # 【P3】临时写区：每个执行者一个 staging/<执行者名>/（**草创时建立**，先审后合）
├── rounds.md + rounds/prompts/   # 轮次日志（管理者维护）+ 轮次提示词存盘目录
└── todolist/               # 问题与阻塞提交处（【P】按路拆文件，一人一文件）
    ├── README.md           # 提交区规则（写入权表 + 条目格式 + 只追加规则）
    ├── manager.md          # 管理者裁决区
    ├── human.md            # 人类授权事项（管理者汇总，人类批复由管理者转记）
    └── <执行者名>.md       # 各执行者提交区（**只追加自己名下条目**，格式 `- [RXX，<执行者名>]`）
```

【P】**todolist 按路拆文件**（人类裁定①，起因单文件多路共写的结构性竞争风险）：`todolist/<执行者名>.md`（各执行者提交区，只追加）/ `manager.md`（管理者裁决）/ `human.md`（人类授权）；规则见 `todolist/README.md`。**单执行者工程可沿用单文件 `todolist.md`**；原单文件在本任务中**冻结存档（只读）**，不再作为写入落点。

【P3】**写区草创建立**：草创时除枝文件外，必须为**每个执行者**建 `task-tree/staging/<执行者名>/`（含 README「产出登记（只追加）」节）与 `task-tree/todolist/<执行者名>.md`——写区是该执行者全部产出的唯一落点（先审后合），缺写区的枝不得标 `READY`。

- 实现枝数量不定：草创时至少建 1 个（或 0 个激活 + 1 个占位），任务展开后由管理者按需**生成新枝**；不活跃的枝**裁剪**（标记 DEPRECATED 并说明原因，不物理删除）。
- 每个 branch 文件必须含固定字段：**Goal（目标）、Tasks（任务分解）、Limitations（边界）、Status（状态）、Results（结果区）、BlockedBy（等待项）、NeedsAuthorization（待授权项）**。模板见 `references/branch-template.md`。
- 【P】**可选字段**（对应字段出自 P0 / P1）：`Status` 区的「**执行者类型**」（作业执行者｜秘书执行者）与「并行组」、`Results` 记录中的「写区路径」「合并状态」——**均为可选**（执行者名已隐含类型、TREE.md 状态表已记执行者与并行组，避免冗余字段）；`Status` 其余必填项与三区结构不变。

### 2.2 Limitations（子枝边界）——预埋结构的关键

每个 branch 的 Limitations 必须写清四条，**草创时就要写好，即使该枝尚未激活**：

1. **允许做什么**：本枝职责范围内可执行的动作清单。
2. **禁止做什么**：明确的不越权清单。例：「不得修改 B3-impl-core 之外的任何源码」「不得代填需要人类授权的字段」「不得修改其他 branch 的产出文件」。
3. **依赖哪些上游枝**：本枝开工前必须等待/读取哪些枝的产出。
4. **产出供哪些下游枝**：本枝结果被谁消费、以什么格式交付。

**写 Limitations 的规则**：宁细勿粗。执行者违反 Limitations 时，管理者在巡检中必须显式记录并处置（纠正/回滚/上报人类）。

【P3】**Limitations 必含写权限三条**（§1）与**禁止项四条**（§2）：§1 须写「写 `staging/<执行者名>/` 产出 / 写本枝三区（不适用先审后合）/ 写 `todolist/<执行者名>.md`（只追加）」；§2 须写「不得写正式工程区域 / 不得写他人写区与其他枝文件 / 不得删除自己的写区 / 不得改本枝 Status·Goal·Tasks·Limitations」。写权限措辞须与 `references/branch-template.md` 的 `Limitations §1/§2` **核心句逐字一致（列表结构可异）**。

### 2.3 Status 状态机（每枝一个状态，管理者维护）

```
INACTIVE（未激活，占位）→ READY（待投递）→ RUNNING（进行中）→ BLOCKED（等待：上游/授权/人工）→ DONE（完成）
（任意状态 ──裁剪──> DEPRECATED：标记原因与时间，文件保留不物理删除）
```

- 只有 `READY` 的枝才允许生成轮次提示词投给执行者。
- 一个执行者同一时刻只允许一个枝处于 `RUNNING`（防止文件冲突，铁律见 §四）。
- 状态变更只由管理者执行；执行者只允许在自己枝的 Results 区追加内容。

【P1】**「一个执行者」= 一个执行者实例（路）**：同一份执行者档案可覆盖同构多路实例（如 `executors/<执行者名>.md` 覆盖 sub-A / sub-B / sub-C 三路）——**同一实例同时只跑一个枝，不同实例可并行**。此语义是并行组（§3.1 第 5 步）成立的前提。

【P1】**状态唯一源规则**：**状态唯一源 = TREE.md 状态表**；枝文件 Status 由管理者在投递 / 收尾时同步（并行组见 §3.1 第 5 步 / §3.6 第 5 条）。`Status` / `Goal` / `Tasks` / `Limitations` 四区**只有管理者可改**——执行者发现状态与实况不一致时**不得自行改 Status**，只在 Results 区呈递（实测教训：同一事实在 TREE / 枝文件 / `PENDING.md` 三处三个值，会致执行者误判「本枝未获授权」）。

## 三、管理者协议（本 skill 调用者执行）

### 3.1 草创阶段（结合 superpowers）

1. **Brainstorm**：按 superpowers 的 brainstorming 流程澄清任务——一次一个问题、提出 2–3 种方案与权衡、分节呈递设计、逐节获批。**HARD GATE：未获人类批准前不得建树。**若环境无 superpowers skill，用等价澄清流程替代（一次一问、提出 2-3 种方案与权衡、分节呈递、逐节获批），并在建树记录（rounds.md R0）中注明所用流程。
2. **扫描既有规范 + 建树**：先扫描项目内既有任务规范/协议文件（docs/、根目录 *.md、用户已打包的规范如 RECURRING_TASK.md 类），确定并存关系（互补/替代/废止）；发现冲突时先向人类呈递并存方案再继续。然后按 §2.1 一次性创建全部目录与文件：预埋骨架 + 每个 branch 的 Goal/Tasks/Limitations + interfaces.md 的对接方案初稿 + rounds.md 的轮次 0 记录。**树骨架与执行者档案复用（若适用）**：建树前先扫描项目内/邻近项目的既有任务树——①执行者档案复用仅限**同一执行者**（同模型同通道），复制到新树 `executors/` 下成独立副本（不跨树共享），档案头部标注「复用自 <X 树> 档案，条目经 <日期> 逐条复核」，过期条目当场修正后再用；②枝骨架只复制模板结构（Goal/Tasks/Limitations 字段框架），旧 Results 与旧内容一律清空重写；③复用动作记入 rounds.md 轮次 0 记录。
【P3】2bis. **建立写区与提交区**：建树同批**为每个执行者**建 `task-tree/staging/<执行者名>/`（含 README「产出登记（只追加）」节）与 `task-tree/todolist/<执行者名>.md`（含 `manager.md` / `human.md`）——**写区与提交区缺一，该枝不得标 `READY`**；单文件 `todolist.md` 不再作为新工程的写入落点（单执行者工程可沿用，多执行者工程一律分路）。
3. **初始任务分解**：管理者按 superpowers 的 writing-plans 精神把各枝任务拆成「单轮 ≤ 20 分钟」的原子任务（参照执行者环境的能力与已知坑拆分，见 §六）。【P6】并产出**枝间依赖矩阵**：每枝列出「依赖哪些枝的产出 / 被哪些枝依赖 / 共享写哪些文件」，存于 B0-governance.md；该矩阵是并行分组的唯一依据——矩阵未列明的依赖视为不存在，但若并行中发现隐性依赖，立即停组呈报，不得自行串行化。
4. **标定 Gate 节点**：按 §3.6 在草创阶段就标定「哪几个节点是人类决策点」，写入 TREE.md 与 B0-governance.md。
【P1】5. **并行组标定**：按依赖矩阵把互不依赖或仅共享只读文件的枝划入同一并行组；TREE.md 状态表加「并行组」列（G1/G2/…）。组内枝可并行投递，组间按依赖顺序串行。**默认全部枝同组（串行）；并行是显式声明，不是默认。** 并行度不超过 interfaces.md 声明的上限（默认 ≤3 路）。
6. **呈递人类确认**：树建成后向人类呈递：branch 图、各枝 limitations 摘要、对接方案、**Gate 节点分布**、**并行组分布与依赖矩阵**、首批可投递枝清单。人类确认后进入轮次阶段。

### 3.2 轮次阶段（管理者→执行者 的对接方式）

每轮循环，管理者做五件事：

1. **选题**：从 `READY` 的枝中选一个，确定本轮任务（结合 `todolist/` 中未决事项与上游枝状态）。**以目的为导向**：若候选轮次只是堆砌文档/纯核验而不推进任务真实目的，直接改选更有价值的动作或向人类提问。
2. **生成轮次提示词**：严格按 `references/round-prompt-template.md` 生成。必须包含：本轮编号、执行者身份、**读树指令（第一步必须读哪些树文件）**、本轮任务（轻量、拆小、每步落盘、单轮 ≤ 20 分钟）、**硬边界（转写自该枝 Limitations）**、环境坑（转写自该执行者档案）、收尾核验清单、呈递格式。【P2】提示词第〇步（会话上下文管理）按**通道模式二选一**：人工投递型写「模式 A」（新建对话、改名「落地环节-N」、旧对话留存）；子 agent 同步型写「模式 B」（独立 Agent 调用、无历史上下文、事实来源只有树文件与提示词）。提示词存盘命名统一为 `round-R<序号>-<执行者名>-prompt.md`（多执行者工程一律带执行者名，避免同名覆盖；单执行者工程可从简）。
3. **投递前存盘**：提示词存盘备份到项目内（如 `task-tree/rounds/prompts/round-R<序号>-<执行者名>-prompt.md`），并更新 rounds.md 与 B0-governance.md。
4. **投递**：通过该执行者的通道投递（computer-use 投 UI / API / 手工粘贴由人类配置）。**投递前必须确认上一轮已结束**（见 §五判定规则）；**投递前权限模式检查**：确认执行者环境权限模式符合人类要求（如「跳过模式」skip permissions 而非「自动模式」accept edits），不符先纠正再投递。两个会话并行写同一批文件是绝对禁止的。
【P2】4bis. **同步通道分支（子 agent 同步调用）**：投递形态为**在单条消息内并列多个 Agent 调用**（调用数 = 并行度），各路独立返回、互不依赖——①「投递前确认上一轮已结束」**天然成立**（Agent 调用阻塞、返回即该路结束），无须额外检查，但**同一执行者实例**在上一轮未返回前不得续投（铁律 1 按执行者逐个适用）；②**无独立权限模式**（权限继承管理者主会话），「权限模式检查」项**免做**——真正的风险是执行者继承管理者工具权限，只能靠**提示词硬边界 + 落盘核验**兜底，不得因免检而放松提示词边界。
5. **投递后验证**：确认提示词完整送达（首末句完整、新轮次已启动、会话名/编号正确）。【P2】**同步通道验证判据**改为：①Agent 调用**返回成功**（报错/超时视为中断，按该轮提示词「中断恢复指引」重投）；②提示词**指定的产出文件已在写区落盘且非空**（以文件实际存在/大小为准，不凭自述）；③返回文本含 ≤5 行中文总结。**管理者填入 `rounds/prompts/PENDING.md` 该路的返回时间、产物路径与核验结论**（投递即登记，返回即回填）。

### 3.3 巡检阶段（定期检查执行者工作）

【P2】**本节含两种模式**，动手前先按 3.3.0 选定模式——**模式 A：独立巡检者模式**（本任务实际模式以 `interfaces.md` 判定为准）；**模式 B：子 agent 同步通道模式**（子 agent 同步通道变体，现行）。两模式**共用**铁律：不打扰运行中轮次、先完成先核验、先审后合、只追加不覆写。

#### 3.3.0 模式选择规则（每轮巡检的第一件事：先定模式，再干活）

| 执行者类型 / 通道形态 | 采用模式 | 依据 |
|---|---|---|
| **人工投递 / 多窗口独立对话 / UI 定时投递**（执行者有自己的人机界面与会话概念） | **模式 A** | 设计稿 v2 · P2；`interfaces.md` §一 通道声明 |
| **子 agent 同步通道**（管理者在**单条消息内并列多个 Agent 调用**、同步返回，无独立会话） | **模式 B** | 子 agent 同步调用；`interfaces.md` §一 |

- **判定依据**：执行者档案的「通道」字段 + `interfaces.md` §一 的通道声明。**以 interfaces.md 为准**（它是通道形态的权威声明），档案提供操作细节。
- **混合工程逐路选择**：同一棵树里部分路为人工投递、部分路为子 agent 时——**按路选模式**，同一轮巡检中 A/B 可并存，按路判定、按路处置；**不要求全工程统一一种模式**，也不得因"另一路用的是 B"而给子 agent 套用 A 的判据。
- **模式变更**（执行者通道更换）→ 属管理者动作：在 `interfaces.md` 记明变更原因与时间，本条条文不改（这正是「双模式并置」的目的——无需回滚条文）。
- **两模式共用铁律**：不打扰运行中轮次、先完成先核验、先审后合、只追加不覆写。

#### 3.3.1 模式 A：独立巡检者模式（本任务实际模式以 `interfaces.md` 判定为准）

巡检由**独立巡检者**执行（人类配置的定时任务驱动，每周期唤醒；非管理者本身）。分工原则——**巡检者做机械循环，管理者做判断与核验**：

1. 巡检者职责仅限机械动作：①对**每个**有 RUNNING 枝的执行者**逐一**做状态判定（规则①②③，判据按各执行者档案，**必须看底层事件记录，不能只看界面**）；②把判定结果经通知通道呈报管理者；③从交接台（`task-tree/rounds/prompts/PENDING.md`）取件投递（新开对话/续投按判定结果）；④投递后验证并回写交接台。**巡检者不做核验、不生成轮次、不写树**。
2. 交接台按执行者分路（`[路·<执行者名>]`），各路独立查重（防双投）、独立锁（「投递中」占位，超时自动解锁判定）。
3. 管理者收到呈报后：完成核验（见 3.4）、生成下一轮提示词、更新树、把待投件放入交接台对应路。
4. 巡检者发现异常（断联、锁超时、通知失败）→ 按既定异常分支上报管理者，不自行处置；无新事件则本轮休眠。
5. 巡检结果回写树由管理者执行：rounds.md 追加轮次记录、更新各枝状态、把执行者呈递的等待项/待授权项并入 `todolist/<执行者名>.md` 与决策清单。

#### 3.3.2 模式 B：子 agent 同步通道模式（子 agent 同步通道，现行）

1. **巡检者环节豁免**：子 agent 为**同步调用**，返回即管理者判知完成，无须第三方巡检者做「运行中 / 已完成 / 中断」判定；**由管理者兼任判定**。独立巡检者机制（3.3.1）**保留条文设计**，待「多窗口独立对话 / 人工投递」执行者场景再启用。
2. **免「取件投递」环节**：无交接台取件动作——管理者在**单条消息内并列投递多个 Agent 调用**即完成投递；交接台降级为**投递登记与审计台**（`PENDING.md` 现状形态，管理者写入、执行者与巡检者只读）。
3. **投递即登记**：投递时在 `PENDING.md` 该路登记「枝 / 并行组 / 提示词存盘路径 / 投递时间 / 状态 = 投递中」；返回后回填「返回时间 / 产物路径 / 核验结论」，使「投递登记 ↔ 写区产出 ↔ 核验结论」形成可审计闭环。
4. **核验仍按 3.4 执行**（管理者独立核验、P5 节流「先完成先核验」、P3 先审后合），**不得因免巡检而降低核验要求**。
5. **异常分支**：调用**报错 / 超时**视为中断 → 按该轮提示词「中断恢复指引」重投；**进度以落盘文件为准**，未落盘步骤重做。
6. **回写树由管理者执行**：rounds.md 追加轮次记录、更新各枝状态（**状态唯一源 = TREE.md 状态表**）、把执行者呈递的等待项/待授权项并入 `todolist/<执行者名>.md` 与决策清单；组内全部枝 `DONE` 后按 §3.6 第 5 条走**汇聚点**。

### 3.4 核验交付（不信自述，独立验证）

执行者报告「完成」不等于完成。管理者必须独立核验：

- 产出文件是否真实存在、内容是否符合任务要求（**以文件实际状态为准，不看执行者自述**）；
- 基线是否被意外改动（关键文件哈希、状态值、依赖清单——**基线一旦变化必须显式记录新值，作废旧值**）；
- 是否越权（对照该枝 Limitations 逐条检查；沙箱不会拦截越权命令，只能靠核验兜底）；
- 是否假阳性（执行者报的「不一致/缺陷」可能只是它核对来源过窄——管理者指定核对来源前必须自己先确认来源覆盖待核字段）。

核验通过才允许枝置 `DONE`；不通过则按**瑕疵分级**处置：①**行文级瑕疵**（不影响数字/判定/合规的表述问题）——不生成纠偏轮次，记入 `todolist/manager.md`，由 BD-deliver 交付时订正并注明；②**数字级/判定级错误**——把问题写进 `todolist/<执行者名>.md` 并生成纠偏轮次；③**合规/越权问题**——按铁律 4 处置（不做、只呈递人类）。

【P5】**核验节流**：并行产出按「先完成先核验」排队，管理者同一时段只核验一路（核验本身不并行）；并行度上限与核验顺序在 interfaces.md 声明。**排队与完成状态登记在 `PENDING.md` 每路的「核验结论」字段**（模式 B 下：先返回先进入核验，不等待未返回的路）。

【P3】**先审后合：核验通过 → 合并指令 → 秘书执行者合并**（本段自 P5 段拆出独立成条）：管理者核验通过 → 下达**合并指令** → 由**秘书执行者**把写区产出**移入正式工程位置**，并**留痕「RXX 合并自 <执行者名> 写区」，记入 rounds.md 与本枝 Results 区**（四要素齐全：合并主体 + 移入正式位置 + 留痕格式 + 留痕落点；铁律 2 P3③ 按同一句模板表述）。**管理者只核验、只下指令，不亲自动手**（铁律 3 原样保持）；核验不通过 → 产出留在写区走纠偏轮次，**正式工程零污染、不生成回滚动作**。

- **合并指令的载体**：合并指令以**轮次提示词**形式生成并投递给秘书执行者，存盘 `task-tree/rounds/prompts/round-R<序号>-merge-secretary-prompt.md`；秘书执行者同样适用写区纪律，其**写入豁免**（凭合并指令可写 `task-tree/<合并目标>/` 与留痕位）见 `references/round-prompt-template.md`「写区落盘要求」第 6 条括注。
- **写区生命周期判定**：已处理完毕（内容**已全部合并或裁定**）的写区**连续 3 轮未被再次使用** → 管理者**在每轮核验后**执行判定并把结论记入 rounds.md，再下删除指令、由秘书执行者执行并留痕；**执行者不得删除自己的写区**（同铁律 2 P3⑤）。**删除前必须逐项确认**（见铁律 2 P3⑤ 确认闸）。

### 3.5 树维护

- **生成新枝**：任务展开需要新模块时，管理者按 branch 模板新建 `B3-impl-*.md`（含完整 Limitations），更新 TREE.md 状态表与 interfaces.md 对接方案。
- **裁剪枝**：某枝不再需要时标记 `DEPRECATED` + 原因 + 时间，保留文件不物理删除（审计用）。
- **调整枝**：Limitations 变更必须记录变更原因与时间；若该枝正 `RUNNING`，调整**必须等本轮结束后**在下一轮提示词中下发（铁律，见 §四）。
- **对接方案**：interfaces.md 随枝间数据流变化同步更新。
- **树只记事实**：树文件（含执行者档案）只记录状态、产出、实测结论、授权与决定；期望、设想、未来规划不入树——应写入管理者记忆或另行立项。树只做树的功能，警惕功能膨胀成大杂烩。
- **树的收官**：全部枝 `DONE` 且 Gate3 通过后——①TREE.md 顶部标注收官时间与一句话结论；②新增「后续接手路径」段（先读什么、证据/产出在哪、如何另立新树复用骨架）；③树文件保留不删（审计与接手用），新任务另立新树。
- **审计只追加**：rounds.md、B0-governance.md、各枝 Results 区的一切记录**只追加、不覆写、不删除**——每条记录带（谁 / 何时 / 原因）；错误用新记录更正，绝不改旧记录（审计链完整性优先于文件整洁）。

### 3.6 Gate 设计（预埋于草创阶段）

**哲学**：「当一切都需要确认时，确认就失去了意义」。人类只应在真正需要决策的节点介入。

1. **草创时标定 Gate**：在 TREE.md 中列明 Gate 节点（建议全程 3 个左右，如：设计定稿 = Gate1、实现完成 = Gate2、验收通过 = Gate3），每个 Gate 写明「为什么这里必须人类决策」。
2. **Gate 之间自动流转**：非 Gate 轮次管理者与执行者自动推进，人类不介入；期间产生的小问题累积进 `todolist/`（管理者裁决 `manager.md` / 人类授权 `human.md`），**不逐轮打扰人类**。
3. **Gate 呈递格式**：到达 Gate 时，管理者独立核验后呈递**质量判断报告 + 固定三选项**——「①通过 ②需要修复（附具体修复项）③放弃」，人类只需三选一。不呈递原始清单、不让人类自由发挥式审阅。
4. **Gate 决定记录**：人类的 Gate 决定写入 B0-governance.md 授权台账，作为后续轮次的硬约束。
【P4】5. **并行组汇聚**：组内全部枝 `DONE` 后，管理者统一独立核验、合并状态，以「组级结论」一次呈递（Gate 或决策清单）。组内单枝的待裁事项累积到汇聚点，不单独呈递——除非该事项阻塞整组。

## 四、铁律（违反任何一条即协议失效）

1. **绝不打扰正在运行的轮次（不打扰 ≠ 不同时）**：执行者 turn 运行期间，不追加消息、不另开会话、不改它正在读写的任何文件；新决定/新结论只写记忆与树，下一轮再下发——即使新结论与运行中轮次正在做的事直接矛盾。**管理者可在执行者运行期间并行做其他工作**（规划、核验其他枝、写树、写记忆、自己的产出落盘），但**不得触碰执行者正在或可能进行工作的文件区域**：本枝 Results/BlockedBy/NeedsAuthorization 三区、`todolist/<执行者名>.md` 该执行者条目、该执行者的临时写区 `task-tree/staging/<执行者名>/`、本轮提示词指定的产出文件；管理者自己的产出用不同文件名落盘，并在记录中标注「与执行者 RXX 并行进行」。
【P2】**同步通道说明**：子 agent 调用**阻塞**管理者——调用期间管理者不产生任何并发写，本铁律由通道语义**自动满足**；但「管理者可在执行者运行期间并行其他工作」在同步调用下**不成立**（除非改用后台并行投递 `run_in_background`，此时恢复人工投递语义、按原铁律执行）。**本铁律按执行者逐个适用，不是全局冻结**：路① 运行中只冻结路① 的接触面，不影响路②/路③ 的判定、核验与投递；**禁止把「有任一路在运行」扩大解释为「全工程冻结」**（实测：三路并行投递与全局冻结矛盾）。
2. **一轮一枝一执行者**：同一执行者同时只跑一个枝；同一枝同时只投给一个执行者；不同执行者并行时——每个执行者只写：自己枝的三区 + 自己的临时写区（`task-tree/staging/<执行者名>/`）+ `todolist/<执行者名>.md` 自己名下的提交条目。**正式工程区域与共享写区禁止执行者直接写入**；禁止两个执行者写同一批文件。【P3】**临时写区（先审后合）**：①**每个执行者分配独立写区 `task-tree/staging/<执行者名>/`（草创时建立，见 §2.1 / §3.1 第 2bis 步）**；本轮全部产出文件一律先落本执行者写区，不得直接写入正式工程区域；②树文件三区（本枝 Results/BlockedBy/NeedsAuthorization）仍由执行者直接追加——三区是各自枝的独立审计文件，不构成并行写冲突；③管理者核验通过后下达合并指令，由**秘书执行者**把写区产出移入正式工程位置并留痕「RXX 合并自 <执行者名> 写区」（记入 rounds.md 与本枝 Results 区，同 §3.4【P3】段）；④核验不通过：产出留在写区走纠偏轮次，正式工程零污染；⑤写区生命周期：已处理完毕的写区连续 3 轮未被再次使用 → 管理者下删除指令、秘书执行者执行并留痕；**删除前必须逐项确认内容已全部合并或裁定并留痕，方可下达删除指令**；执行者不得删除自己的写区。
3. **不越权是双向的**：执行者不干枝外的事；管理者不干执行者的活（管理者只规划、核验、记录，不替执行者写产出——除非人类明确授权本轮由管理者直接落地）。
4. **需要人类授权的事，一律不做、只呈递**：任何超出 Limitations 或超出人类已授权范围的重大动作（装依赖、外发数据、扩大样本、动基线、删文件等），执行者和管理者都不得自行决定，只能写进 NeedsAuthorization 并呈递人类。
5. **一切以目的为导向**：判断每轮工作是否服务于任务真实目的；只为流程完整而堆砌的轮次应被质疑和替换。

## 五、执行者协议（下发给执行者大模型）

执行者不调用本 skill——它收到的是管理者生成的**轮次提示词**。管理者在草创阶段把下面的协议全文写入树（`task-tree/WORKER_PROTOCOL.md`，模板见 `references/worker-protocol.md`），并在每轮提示词中引用。协议要点：

1. **开工前必读**（顺序固定）：① TREE.md → ② 本枝文件（重点 Goal 与 Limitations）→ ③ interfaces.md 与本枝相关部分 + 上游枝 Results 区 → ④ `todolist/<执行者名>.md`——**你自己的提交区**（一人一文件）→ ⑤ WORKER_PROTOCOL.md 执行者协议全文（在 task-tree/ 下）。读完先在心里/回复开头简述「本轮我在哪个枝、干什么、边界是什么」，再读本轮提示词全文动手。
2. **干活的边界规则**：
   - 只干本轮提示词指派的活，**不干别的 branch 的活**——即使发现别的枝有明显问题，也只能记入 `todolist/<执行者名>.md` 或在本轮结果中呈递，不得顺手处理；
   - 规定多少活干多少事，不擅自扩大范围、不擅自"顺手优化"；
   - 遇到问题/阻塞：写进 `task-tree/todolist/<执行者名>.md`（**你自己的提交区**），并在结果中呈递；不得自行绕过 Limitations 去解决；
   - 需要人类授权的事：**不做**，写进本枝 NeedsAuthorization 区，并在结果中呈递。
   - 【P3】共享写区纪律：**产出先落自己的写区** `task-tree/staging/<执行者名>/`，不得直接写入正式工程区域（先审后合，见铁律 2）；**todolist 按路拆文件**——只追加自己名下条目（`- [RXX，<执行者名>]` 格式），不得改他人文件（`manager.md` / `human.md` / 其他 `sub-*.md` 一律不写）；管理者裁定统一写在 `manager.md`。
3. **干完必做**（呈递结果）：
   - 在本枝 Results 区追加本轮记录（格式：轮次号、做了什么、产出文件清单、**写区路径**、核验结果、结论；只追加不覆写）；
   - 更新本枝 BlockedBy 区：列出「等待别的枝完成才能继续」的任务及依赖的枝名；
   - 更新本枝 NeedsAuthorization 区：列出需要人类授权的事项及理由；
   - 【P3】在自己的写区 `staging/<执行者名>/README.md` 的「产出登记（只追加）」节追加一行（产出文件名 + 字节数 + 状态），使「写区产出 ↔ 投递登记」闭环；
   - 输出不超过 5 行的中文总结作为回复结尾。
4. **语言**：全程用管理者在提示词中指定的语言（默认中文）；路径、命令、代码标识符、已固化的流程名词可保留原文。

## 六、执行者档案（管理者必须为每个执行者维护一份）

存入 `task-tree/executors/<执行者名>.md`。档案内容：

1. **通道**：如何投递提示词、如何读回结果（UI 操作路径 / API / 手工粘贴 / **子 agent 同步调用**）。
2. **状态判定方法**：如何判断它运行中/已完成/中断（底层事件文件的路径与判据、UI 目视判据、不可靠信号的警告）。
3. **能力边界**：能否读文件、能读什么格式、有无视觉/联网能力、单轮时长上限、输出长度限制。
4. **权限模式要求**：人类对执行者环境的权限模式要求（如：跳过模式而非自动模式）——投递前检查项。
5. **已知环境坑**：投递与执行中踩过的坑及绕行写法（如：命令参数限制、路径转义、落盘语法、剪贴板编码等）。
6. **工作模式画像**（历轮实测总结，任务设计依据）：强项（可放心委派）/ 弱项（任务设计必须防御）/ 任务设计启示（小步多轮、每步落盘等）。未实测的标注「未经实测——首轮后按实测回写」。
7. **调度权**：人类可授权管理者在轮次间自行调度（更换执行者/模型/思维强度）；每次调度变更必须记录于本档案（谁/何时/旧值→新值/原因）与 B0-governance.md 授权台账。
8. **语言要求**：该执行者回复语言、思考摘要语言。

【P2】**子 agent 型执行者的两条适配**：①第 4 项「权限模式要求」写「**继承主会话，无独立权限模式，投递无权限检查项**」（§3.2 第 4bis 步据此免做权限检查）；②第 1 项「通道」须写明「**子 agent 同步调用**（管理者单条消息内并列投递、同步返回）」，通道形态与 `interfaces.md` §一 一致——档案提供操作细节，**`interfaces.md` 为通道形态权威**（决定 §3.3 采用模式 A 还是模式 B）。

**档案是执行者环境知识的唯一权威**：操作细节（UI 坐标、快捷键、剪贴板编码等）一律写入档案或 executors/ 目录内附件文件；不得引用树外的记忆文件。

**档案的每一条都来自实测，标注实测日期；未实测的标记「未经实测」。** 管理者每次巡检发现新坑必须回写档案——这正是「定时任务方法经过多轮调整后完善」的机制：环境知识不靠记忆，靠档案逐轮沉淀。

## 七、定时任务交流协议（管理者↔执行者 的自动化对接）

人类可通过定时任务驱动管理者定期巡检。定时任务的提示词由管理者生成（结合执行者档案与树当前状态），存盘后交给人类配置。完整协议见 `references/scheduler-protocol.md`，核心流程：

```
触发 → 铁律检查（运行中则不干预，按执行者逐个判定）→ 判定执行者状态（逐路，规则①②③）
     → 核验交付/生成下一轮提示词 → 存盘 → 投递 → 投递后验证
     → 回写树与记忆 → 向人类呈递累积决策清单
```

【P2】**本协议含双模式，先按「第 0 步 0.1」选定模式**：**模式 A 独立巡检者版**（定时任务唤醒独立巡检者、交接台分路取件投递；本任务实际模式以 `interfaces.md` 判定为准）与**模式 B 子 agent 同步通道版**（巡检者环节豁免、管理者兼判定、免取件投递、`PENDING.md` 降级为投递登记台）；两模式共用铁律（不打扰运行中轮次、先完成先核验、先审后合、只追加不覆写），**分叉点仅「第 2 步判定判据」「第 4 步投递形态」两处**。**子 agent 通道的工程整节不启用定时任务链路**（管理者在会话内同步投递）；模式选择规则与本 skill §3.3.0 **同源**，措辞以 `references/scheduler-protocol.md` 0.1 为准。

**决策清单呈递规则**（每轮必做，人类可能缺席多轮）：
- 先读上轮记录找历轮未决事项，**再用实测状态核对是否已解决**（不照搬旧清单）；
- 累积呈递，用列表不用表格；每项写清五要素：①要做什么 ②在哪做（路径+字段） ③可选值与限制 ④做完走哪条分支 ⑤已等待轮数；
- 标注阻塞等级（阻塞主线 / 可延后）；
- 【P4】并行组内单枝待裁事项**累积到汇聚点**一并呈递；仅**阻塞整组**的事项即刻呈递。

## 八、调用本 skill 时的开场动作（Checklist）

1. 确认任务是否适用本协议（大型、多轮、有执行者模型参与）；不适用则明确告知用户并退出本协议。
2. 确认人类已分配好执行者模型及通道；未分配则先向人类呈递「需要哪些角色、哪些模型、哪些通道」的清单，等分配完成再继续。
3. 按 §3.1 进入草创阶段（superpowers brainstorm → 建树 → 人类确认），**未获批不得建树**。
4. 建树后按 §3.2–3.5 进入轮次循环，直至所有枝 `DONE` 或人类叫停。

---

